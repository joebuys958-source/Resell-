# Premium Inventory App (Vite + React)
## Setup
1. unzip or clone repo
2. cd premium-inventory-app
3. npm install
4. npm run dev
5. Build: npm run build -> upload /dist to GitHub Pages or deploy to Vercel/Netlify
